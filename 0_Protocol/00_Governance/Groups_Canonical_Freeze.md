# Groups Canonical Freeze Declaration

- **File (default / code path):** `2_Data/metadata/groups_canonical.csv`
- **Generated by (current):** `3_Code/src/preprocess/build_groups_canonical_v2.py`
- **Source (current SSOT):** `2_Data/processed/Radiology_Curriculum_Weight_Factor_v2.xlsx`
- **Definition (binding):** `group_weight_sum = Σ weight_factor` (**NO normalization**)

## Versioned Snapshots (Legacy preservation)

This repository preserves legacy group universes for interpretability and S0 continuity:

- **Legacy (v1) snapshot (read-only):**
  - `2_Data/metadata/legacy/groups_canonical_v1.csv` (+ `.sha256`, `.meta.json`)
- **Operational (v2) snapshot (default):**
  - `2_Data/metadata/groups_canonical_v2.csv` (+ `.sha256`, `.meta.json`)
  - Promoted to **default name** `groups_canonical.csv` when the pipeline runs on v2.

## Freeze Scope
- Applies to: S0, S1, FINAL
- Re-run only if:
  1) curriculum objectives change
  2) grouping rule changes
  3) weight factor definition changes

## Declared by
- Date: 2025-12-27
- Status: Frozen (Canonical) — **v2 snapshot**

## Operational Rule (Implementation-aligned)

- Downstream stages MUST consume `2_Data/metadata/groups_canonical.csv` as the single input universe.
- S0 interpretation MAY reference the legacy snapshot under `2_Data/metadata/legacy/` when needed.
- Any change that alters the group universe requires:
  - explicit protocol revision,
  - new snapshot generation,
  - and updated `.sha256` + `.meta.json`.
