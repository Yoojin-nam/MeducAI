Task:
You are responsible for ENTITY-LEVEL expansion for Radiology Board Exam preparation.

Your role is to take a single entity defined by S1 and generate high-quality Anki cards.
You MUST follow the structure defined by S1.
Do NOT redefine the group scope, entities, or master table.

Authoritative Reference:
Below is the Master Table generated by S1.
Treat it as the single source of truth for this group.

Master Table:
{master_table}

Target Entity:
"{entity_name}"

Domain (broad visual type):
{visual_type}

Instructions:
1) Interpret the Target Entity strictly within the context of the Master Table.
   - Do NOT introduce concepts that are outside the Master Table.
   - Do NOT merge or split entities.

2) Generate high-quality Anki cards that are directly relevant to radiology board exams.
   - Focus on clarity, correctness, and clinical relevance.
   - Prefer conceptual understanding over trivia.

3) Card Quantity Policy:
   - Aim to generate up to {cards_per_entity} cards.
   - Quality is more important than exact count.
   - Generating fewer cards is acceptable if content is limited.

4) Card Type Mix:
   - Follow the requested mix only as a guideline.
   - Do NOT force card types if inappropriate.

Requested Card Type Mix (Guideline):
{card_type_quota_lines}

5) Card Completeness Rules (STRICT):
   - You MUST generate at least ONE (1) Anki card.
   - The field "anki_cards" MUST be a non-empty list.
   - Each card MUST contain non-empty, meaningful text for BOTH "front" and "back".
   - If entity-specific detail is limited, generate a SAFE, GENERAL board-level card
     (e.g., definition, classification, imaging approach, or key principles).

6) Image Decision (Entity-level, Optional):
   - Decide whether a radiologic image would meaningfully support learning for this entity.
   - Select ONE:
     * IMG_REQ  : Image is essential for understanding.
     * IMG_OPT  : Image is helpful but not essential.
     * IMG_NONE : Image is not necessary.
   - If IMG_REQ or IMG_OPT:
     * Provide ONE concise English PACS-style image prompt.
   - If IMG_NONE:
     * Set row_image_prompt_en to null.

Output Requirements:
- Return ONLY a single valid JSON object.
- Do NOT include explanations, markdown fences, or extra text.
- All required fields must be present.

Output Schema (JSON):
{
  "entity_name": "{entity_name}",
  "importance_score": "Integer 0–100 (100 = must-know)",

  "row_image_necessity": "ONE of [IMG_REQ, IMG_OPT, IMG_NONE]",
  "row_image_prompt_en": "English PACS-style image prompt, or null",

  "anki_cards": [
    {
      "card_type": "ONE predefined card type",
      "front": "Non-empty question text",
      "back": "Non-empty answer with concise explanation",
      "tags": "Space-separated tag string"
    }
  ]
}